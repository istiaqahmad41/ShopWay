import 'package:flutter/material.dart';

class SubAdminPage extends StatelessWidget {
  const SubAdminPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Subadmin'),),);
  }
}
